---
'@backstage/plugin-catalog': minor
---

Exported `NfsDefaultCatalogPage` for use in custom new frontend system pages. This variant renders without the legacy page shell, making it suitable for use inside a `PageBlueprint`.
